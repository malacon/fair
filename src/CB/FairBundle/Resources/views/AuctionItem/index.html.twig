{% extends 'FairBundle::layout.html.twig' %}

{% block content -%}
    <h1>Auction Items List</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Description</th>
                <th>Quantity</th>
                <th>Cost</th>
                <th>User</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('auctionitem_show', { 'id': entity.description }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantity }}</td>
                <td>{{ entity.cost }}</td>
                <td>{{ entity.user }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('auctionitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('auctionitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('auctionitem_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
