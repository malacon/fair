{% extends 'FairBundle::layout.html.twig' %}

{% block content -%}
    <h1>Family creation</h1>

    <form action="{{ path('admin_family_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_family') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
